import SettingsPage from "@/cmms/SettingsPage";

export const metadata = {
  title: "Settings | Computerized Maintenance Management System (CMMS)",
};

export default function Settings() {
  return <SettingsPage />;
}
